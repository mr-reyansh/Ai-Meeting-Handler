# Database Configuration (SQLite, no username/password required)
# Optional: path to sqlite file. Default is backend/database.sqlite
DB_PATH=./database.sqlite

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Server Configuration
PORT=5000
NODE_ENV=development
